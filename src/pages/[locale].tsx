import { getStaticPaths, getStaticProps } from 'lib/i18next/utils/get-static';
import Home from 'pages/index';

export { getStaticPaths, getStaticProps };

// eslint-disable-next-line import/no-default-export
export default Home;
